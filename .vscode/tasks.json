{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Auto Git Commit & Push",
      "type": "shell",
      "command": "powershell",
      "args": [
        "-NoProfile",
        "-Command",
        "& { $msg = 'Auto commit on ' + (Get-Date -Format \\\"yyyy-MM-dd HH:mm:ss\\\"); git add .; git commit -m $msg; git push }"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": []
    }
  ]
}
